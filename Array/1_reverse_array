void reverseArray(vector<int> &arr) {
    int l=0,r=arr.size()-1;
    while(l<r){
        int temp;
        temp=arr[l];
        arr[l]=arr[r];
        arr[r]=temp;
        l++;
        r--;
        
    }
}